# Requirements minimalistas para Render.com
# SEM dependências que requerem compilação Rust ou C pesada

# API Framework (essencial)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23

# HTTP Client (para API externa)
httpx==0.25.2
certifi==2023.11.17
urllib3==2.1.0

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6

# Logging
loguru==0.7.2

# IMPORTANTE: Removidas dependências pesadas:
# - pandas, numpy (requerem compilação)
# - selenium, playwright (requerem browsers)
# - openpyxl, reportlab (não essenciais para API básica)
# - pytest (não necessário em produção)

