# Requirements ULTRA minimalistas para Render.com
# Apenas o essencial para a API funcionar
# SEM dependências que requerem compilação Rust ou C pesada
# Versões ANTIGAS e ESTÁVEIS com wheels pré-compilados garantidos

# API Framework (versões antigas e estáveis)
fastapi==0.95.2
uvicorn[standard]==0.22.0
pydantic==1.10.7
pydantic-settings==1.10.1

# Database (SQLAlchemy + driver PostgreSQL binário)
sqlalchemy==1.4.46
psycopg2-binary==2.9.6

# HTTP Client (aiohttp é puramente Python, sem Rust - assíncrono)
aiohttp==3.8.5

# Utilities
python-dotenv==0.21.0
python-multipart==0.0.5

# Logging (versão antiga sem Rust)
loguru==0.6.0

# Scheduling
schedule==1.2.0

# NOTA CRÍTICA: 
# - Versões ANTIGAS usadas para garantir wheels pré-compilados
# - Nenhuma dependência transitiva deve requerer Rust
# - Se ainda falhar, pode ser necessário usar requests ao invés de httpx




